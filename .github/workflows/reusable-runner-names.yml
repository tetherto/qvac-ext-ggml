# AUTO-GENERATED by .github/scripts/sync-runner-names.mjs
# Source of truth: .github/runners.yaml
# Do not edit this file by hand.

name: Runner names

on:
  workflow_call:
    outputs:
      ubuntu_2204:
        description: "Ubuntu 22.04 hosted x64"
        value: ${{ jobs.export.outputs.ubuntu_2204 }}
      ubuntu_2204_arm:
        description: "Ubuntu 22.04 hosted arm64"
        value: ${{ jobs.export.outputs.ubuntu_2204_arm }}
      selfhosted_linux_x64_nvidia:
        description: "Linux x64 NVIDIA GPU"
        value: ${{ jobs.export.outputs.selfhosted_linux_x64_nvidia }}
      selfhosted_linux_x64_nvidia_coopmat2:
        description: "+ Vulkan coopmat2"
        value: ${{ jobs.export.outputs.selfhosted_linux_x64_nvidia_coopmat2 }}
      selfhosted_macos_arm64:
        description: "Apple Silicon (Metal/Vulkan)"
        value: ${{ jobs.export.outputs.selfhosted_macos_arm64 }}

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      ubuntu_2204: ${{ steps.export.outputs.ubuntu_2204 }}
      ubuntu_2204_arm: ${{ steps.export.outputs.ubuntu_2204_arm }}
      selfhosted_linux_x64_nvidia: ${{ steps.export.outputs.selfhosted_linux_x64_nvidia }}
      selfhosted_linux_x64_nvidia_coopmat2: ${{ steps.export.outputs.selfhosted_linux_x64_nvidia_coopmat2 }}
      selfhosted_macos_arm64: ${{ steps.export.outputs.selfhosted_macos_arm64 }}
    steps:
      - name: Export runner labels
        id: export
        run: |
          echo 'ubuntu_2204=ubuntu-22.04' >> "$GITHUB_OUTPUT"
          echo 'ubuntu_2204_arm=ubuntu-22.04-arm' >> "$GITHUB_OUTPUT"
          echo 'selfhosted_linux_x64_nvidia=["self-hosted","Linux","X64","NVIDIA"]' >> "$GITHUB_OUTPUT"
          echo 'selfhosted_linux_x64_nvidia_coopmat2=["self-hosted","Linux","X64","NVIDIA","COOPMAT2"]' >> "$GITHUB_OUTPUT"
          echo 'selfhosted_macos_arm64=["self-hosted","macOS","ARM64"]' >> "$GITHUB_OUTPUT"
